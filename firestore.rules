rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write for all collections to enable server-side processing using the Client SDK
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
